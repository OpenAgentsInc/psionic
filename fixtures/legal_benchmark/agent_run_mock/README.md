# Mock Agent Run Fixture

This directory records the stable shape of the Rust legal benchmark agent run
artifacts. The unit test in `legal_benchmark_agent.rs` generates equivalent
artifacts in a temporary directory with a deterministic mock model.

The hashes here are illustrative fixture values; live runs compute them from
the generated JSON artifacts.
