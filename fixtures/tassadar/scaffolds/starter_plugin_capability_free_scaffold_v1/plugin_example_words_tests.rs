// TODO: merge these tests into the runtime test module in
// `crates/psionic-runtime/src/tassadar_post_article_starter_plugin_runtime.rs`.

#[test]
fn example_words_success_case_is_bounded_and_typed() {
    // TODO: call invoke_example_words_json_packet with one success packet and
    // assert the typed response fields plus output schema id.
}

#[test]
fn example_words_runtime_bundle_matches_committed_truth() {
    // TODO: compare build_example_words_runtime_bundle() against the committed
    // fixture once the bundle writer lands.
}
