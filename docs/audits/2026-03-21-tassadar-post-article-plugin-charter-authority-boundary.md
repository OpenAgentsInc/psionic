# TAS-197 Audit

`TAS-197` closes the plugin charter, authority-boundary, and platform-law
tranche above the rebased post-`TAS-186` machine.

The canonical artifact is now
`fixtures/tassadar/reports/tassadar_post_article_plugin_charter_authority_boundary_report.json`.
It binds the plugin lane to the same canonical bridge machine identity,
canonical route, continuation contract, and computational-model statement as
the rebased carrier, and it inherits the pre-plugin control-plane
decision-provenance proof plus the post-article closeout and plugin-capability
boundary as hard dependencies.

The report freezes the plugin charter as an operator/internal-only capability
layer: proof-bearing artifacts remain distinct from observational audits, the
observer model stays machine-readable, the data/control/capability plane split
stays explicit, packet-local and instance-local plugin state stay separate
from host-backed durable receipts and weights-owned control state, and host
may execute declared capabilities but may not decide workflow. It also freezes
semantic-preservation, choice-set integrity, resource transparency, scheduling
ownership, no-externalized-learning, bounded-effectful plugin language,
closed-world operator curation, anti-interpreter-smuggling, downward
non-influence, and governance-receipt laws without widening the current claim
surface.

The operator-readable summary now lives at
`fixtures/tassadar/reports/tassadar_post_article_plugin_charter_authority_boundary_summary.json`,
the provider receipt now lives at
`crates/psionic-provider/src/tassadar_post_article_plugin_charter_authority_boundary.rs`,
and the dedicated checker now lives at
`scripts/check-tassadar-post-article-plugin-charter-authority-boundary.sh`.

This tranche is necessary for later weighted-plugin work, but it is still not
the manifest and hot-swap contract, packet ABI and PDK, runtime API, receipt
family, controller trace loop, publication/trust gate, bounded weighted plugin
platform closeout, served/public universality, or arbitrary software
capability closeout.
